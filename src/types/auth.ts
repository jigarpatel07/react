export type SignInValue = {
    email: string;
    password: string;
};
export type SignUpValue = {
    userName: string;
    email: string;
    password: string;
    gender: string;
    profession: string;
    interests: string[]
};